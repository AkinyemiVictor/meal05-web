export const metadata = {
  title: "Home | Meal05",
  description: "Discover popular groceries, seasonal picks, fresh arrivals and curated Meal05 collections.",
  alternates: { canonical: "/home" },
};

export default function HomeLayout({ children }) {
  return children;
}
